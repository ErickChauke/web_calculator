// Constants used throughout the calculator application

// Maximum number of digits that can be displayed
const MAXIMUM_DISPLAY_DIGITS: number = 12;

// The initial value shown on the display
const INITIAL_DISPLAY_VALUE: string = '0';

// Character used for decimal point
const DECIMAL_POINT: string = '.';

// The value representing zero
const ZERO_VALUE: string = '0';

// Number base for parsing (decimal system)
const DECIMAL_BASE: number = 10;

// Percentage conversion factor (converts percentage to decimal)
const PERCENTAGE_DIVISOR: number = 100;

export {
    MAXIMUM_DISPLAY_DIGITS,
    INITIAL_DISPLAY_VALUE,
    DECIMAL_POINT,
    ZERO_VALUE,
    DECIMAL_BASE,
    PERCENTAGE_DIVISOR
};
